 
 doctype html
html
  head
    title Get Game Recommendation
  body
    h1 Find a Game Based on Your MBTI

    form(action="/recommend", method="POST")
      label Select Your MBTI:
      select(name="mbti")
        option(value="INTJ") INTJ
        option(value="ENTP") ENTP
        option(value="ISFP") ISFP
        option(value="ESFJ") ESFJ
        // Add all types as needed
      button(type="submit") Recommend Game

    if result
      h2 Recommendation:
      p= result
